
## 登录页重设计（方案B：3D 玻璃拟态）
- 用户从 A(Odoo极简)/B(3D玻璃)/C(暗色高对比) 三方案中批准 B，预览稿 outputs/login-preview-b.html。
- 正式改动仅两处：src/auth/Login.jsx（登录逻辑不变，仅换标记：lg-* 类名 + SVG 等距3D场景 + 玻璃拟态卡片）与 src/styles.css（删除旧 .login-* 规则，新增 lg-* 样式及响应式；同步清理 131/244/246 行中对旧 login 类的引用）。
- 其余前端、后端代码未动。vite build 验证通过（dist 被沙箱保护，改用 --outDir .verify-build --emptyOutDir=false 验证后删除）。
